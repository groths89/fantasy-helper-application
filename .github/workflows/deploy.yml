name: Build and Deploy
on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build and Push Images
        run: |
          # Build Frontend
          docker build -t ghcr.io/${{ github.repository_owner }}/fantasy-helper-application-ui:latest ./frontend
          docker push ghcr.io/${{ github.repository_owner }}/fantasy-helper-application-ui:latest
          
          # Build Backend
          docker build -t ghcr.io/${{ github.repository_owner }}/fantasy-helper-application-api:latest ./backend
          docker push ghcr.io/${{ github.repository_owner }}/fantasy-helper-application-api:latest

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: SSH into Lightsail and Deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.LIGHTSAIL_IP }}
          username: ec2-user
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd ~/docker/fantasy-helper-application
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            docker compose pull
            docker compose up -d --remove-orphans
            docker image prune -f
